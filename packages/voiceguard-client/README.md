# voiceguard-client

Dependency-free request builder and fail-closed response parser for applications
calling a VoiceGuard Unity AI Gateway Model Service. The application supplies
its own authenticated HTTP transport.
