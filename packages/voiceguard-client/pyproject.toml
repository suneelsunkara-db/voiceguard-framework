[project]
name = "voiceguard-client"
version = "0.1.0"
description = "Dependency-free request and response contracts for VoiceGuard clients"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
